---
description: Specifies the tech stack and APIs used, including mock data for preview mode.
globs: 
alwaysApply: false
---
Follow the Protocol Pro Tech Stack Document for technology choices.
Use mock data and local state for all backend interactions in preview mode.
Keep API calls abstracted in the services/ directory for future backend changes.
Ensure styles are defined with StyleSheet.create and avoid inline styles.
Structure the project to support future additions like UI libraries or payment providers.